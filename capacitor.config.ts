import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'entrega-final-2023',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
